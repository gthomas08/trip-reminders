# Database Configuration (for Docker)
DATABASE_NAME=trip_reminders_api_development
DATABASE_USER=trip_reminders
DATABASE_PASSWORD=trip_reminders_dev
DATABASE_HOST=localhost
DATABASE_PORT=5432

# Redis Configuration (for Docker)
REDIS_URL=redis://localhost:6379/0
